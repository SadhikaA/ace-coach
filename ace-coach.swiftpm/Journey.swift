import SwiftUI

let journey = Journey(pages: [
    JourneyPage(
        choices: [
            Choice(text:"Racquet 1", destination: 1), 
            Choice(text:"Racquet 2", destination: 2)]
    ), 
        
    JourneyPage(
    
    ) ])
