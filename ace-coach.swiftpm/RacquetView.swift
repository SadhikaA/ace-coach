import SwiftUI

struct RacquetView: View {
    var body : some View {
        VStack {
            Text("Step 1: Choose your racquet.")
        } 
    }   
}

struct RacquetView_Previews: PreviewProvider {
    static var previews: some View {
        RacquetView()
    }
} 
