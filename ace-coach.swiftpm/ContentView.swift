import SwiftUI

struct ContentView: View {
    var body: some View {        
        VStack {
            Text("ace").font(.largeTitle).padding()
            Text("Your personal tennis coach").padding()
        }
    }
}
